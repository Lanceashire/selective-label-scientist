import { render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
vi.mock("./bridge", () => ({ DesktopBridge: { subscribeScientistEvents: vi.fn().mockResolvedValue(() => undefined), getSession: vi.fn().mockResolvedValue({ session_id:"session-workbench", status:"RESEARCH", research_plan_locked:false, final_evaluation_revealed:false, hypotheses:[{hypothesis_id:"h1",content:"test hypothesis",status:"ACTIVE",version:1}], plans:[{plan_id:"p1",recipe_json:"{}"}], runs:[{run_id:"r1",policy:"Random",budget:10,status:"COMPLETED",round_end:2}] }) } }));
vi.mock("./ScientistControl", () => ({ ScientistControl: () => <div>Agent 输入区</div> }));
import { ResearchWorkbench } from "./ResearchWorkbench";
const session = { session_id:"session-workbench", status:"RESEARCH", candidates:{}, schema:{row_count:120,column_count:6,sample_limit_for_llm:50,columns:{}}, domain_spec:{domain_name:"selective labels",historical_decision:{column:"decision",observed_action_values:["reviewed"]},outcome:{column:"outcome"},observation_cost:{column:"cost"}} };
describe("ResearchWorkbench", () => it("shows persisted session evidence with data environment and Oracle isolation in one page", async () => { render(<ResearchWorkbench session={session} />); expect(await screen.findByText("test hypothesis")).toBeTruthy(); expect(screen.getByText("数据环境")).toBeTruthy(); expect(screen.getByText("研究时间线")).toBeTruthy(); expect(screen.getByText("当前证据")).toBeTruthy(); expect(screen.getByText(/Oracle LOCKED/)).toBeTruthy(); expect(screen.getByText("Agent 输入区")).toBeTruthy(); }));