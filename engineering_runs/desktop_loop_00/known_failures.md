# Known failures / constraints

None for Loop 0.

Environment note: `rustc` and `cargo` are not installed, so a Tauri window or installer cannot be built on this machine until the Rust toolchain is made available. This is not a failed Loop 0 criterion because Loop 0 contains no desktop build.
