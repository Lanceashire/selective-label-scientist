# Loop 4 dynamic-environment gate

Status: PASS

The environment is multi-round: fit → predict → open a fresh batch → policy select → reveal selected labels → update → refit. Research observations do not contain hidden or oracle labels. Finalization closes the environment permanently.
