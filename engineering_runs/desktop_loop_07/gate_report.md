# ECOMIC Desktop — Loop 7 Gate Report

**Status:** PASS  
**Next loop allowed:** Yes

Loop 7 integrates the existing ECOMIC scientist workflow with the vendored Pi Agent Core and exposes it through the Windows desktop GUI. The production launcher only provides audited typed research tools; it blocks Oracle/hidden-label access, arbitrary shell tools and injected final metrics.

The automated Mock Provider evidence is two-layered: a Pi Agent Core mock stream demonstrates that researcher-visible run feedback changes the next call to `revise_hypothesis`, while the backend workflow persists the required dataset, session, hypotheses, plans, runs, evidence comparison, final evaluation and final report.

The current custom OpenAI-compatible provider uses the same Pi provider implementation for connection testing and agent execution. Credentials remain in Windows Credential Manager and are passed to child processes only as inherited environment variables.