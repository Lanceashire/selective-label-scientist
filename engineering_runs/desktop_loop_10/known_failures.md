# Known failures

No Loop 10 product-blocking failures. WDIO reports an EdgeDriver-download fallback and a post-session mock-store cleanup warning after all assertions pass; both are test-environment warnings, not product failures.