# Loop 16 Gate — PASS

The NSIS installer installs the final ECOMIC Desktop application and its bundled runtime. The application process starts and its Pi typed-tool smoke runs without a developer runtime. Silent uninstall exits zero and removes only the installed application directory.